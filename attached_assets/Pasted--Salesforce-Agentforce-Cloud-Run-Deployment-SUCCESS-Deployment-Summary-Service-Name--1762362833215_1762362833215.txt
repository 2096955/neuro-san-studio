# Salesforce Agentforce Cloud Run Deployment - SUCCESS ✅

## Deployment Summary

**Service Name**: `salesforce-agentforce`  
**Region**: `us-central1`  
**Status**: ✅ **DEPLOYED AND OPERATIONAL**

**Service URL**: `https://salesforce-agentforce-534348290993.us-central1.run.app`

## Deployment Details

- **Revision**: `salesforce-agentforce-00001-r9x`
- **Platform**: Managed Cloud Run
- **Port**: 8080 (Cloud Run compatible)
- **Memory**: 512Mi
- **CPU**: 1
- **Timeout**: 300 seconds
- **Max Instances**: 10
- **Authentication**: Public (unauthenticated)

## Health Check Results

✅ **Health Endpoint**: `/health`
```json
{
    "status": "healthy",
    "agent_id": "salesforce-agentforce-agent",
    "salesforce_configured": true,
    "message": "OAuth connection successful."
}
```

✅ **Root Endpoint**: `/`
```json
{
    "agent_id": "salesforce-agentforce-agent",
    "status": "ready"
}
```

## Configuration

### Environment Variables (Set)
- `AGENTFORCE_MY_DOMAIN_URL`: https://epsdc-dev-ed.develop.my.salesforce.com
- `AGENTFORCE_AGENT_ID`: 0XxfI0000003MjxSAE
- `AGENTFORCE_SALES_AGENT_ID`: 0XxfI0000003N1hSAE
- `AGENTFORCE_SERVICE_AGENT_ID`: 0XxfI0000003MjxSAE
- `VWI_AGENTFORCE_SALES_AGENT_ID`: 0XxfI0000003NEbSAM
- `VWI_AGENTFORCE_SERVICE_AGENT_ID`: 0XxfI0000003NCzSAM
- `AGENTFORCE_CLIENT_ID`: (configured)
- `AGENTFORCE_CLIENT_SECRET`: (configured)

**Note**: Credentials are stored as environment variables. For production, consider migrating to Secret Manager once IAM permissions are available.

## Test Commands

### Health Check
```bash
curl https://salesforce-agentforce-534348290993.us-central1.run.app/health
```

### Root Endpoint
```bash
curl https://salesforce-agentforce-534348290993.us-central1.run.app/
```

### Execute Task
```bash
curl -X POST https://salesforce-agentforce-534348290993.us-central1.run.app/execute \
  -H "Content-Type: application/json" \
  -d '{
    "task_id": "test-123",
    "prompt": "Hello from Cloud Run",
    "context": {}
  }'
```

## View Service Details

```bash
gcloud run services describe salesforce-agentforce \
  --region us-central1
```

## View Logs

```bash
gcloud run services logs read salesforce-agentforce \
  --region us-central1

# Follow logs in real-time
gcloud run services logs tail salesforce-agentforce \
  --region us-central1
```

## Update Service

### Update Environment Variables
```bash
gcloud run services update salesforce-agentforce \
  --region us-central1 \
  --set-env-vars "KEY=value"
```

### Update Resource Allocation
```bash
gcloud run services update salesforce-agentforce \
  --region us-central1 \
  --memory 1Gi \
  --cpu 2
```

### Redeploy
```bash
cd /Users/anthonylui/SubAgents/gemini-salesforce-agent
gcloud run deploy salesforce-agentforce \
  --source . \
  --region us-central1
```

## Integration

### Use in Other Services
```python
import requests

SERVICE_URL = "https://salesforce-agentforce-534348290993.us-central1.run.app"

# Health check
response = requests.get(f"{SERVICE_URL}/health")
print(response.json())

# Execute task
response = requests.post(
    f"{SERVICE_URL}/execute",
    json={
        "task_id": "integration-test",
        "prompt": "Query Salesforce for account information",
        "context": {
            "agent_id": "0XxfI0000003MjxSAE"
        }
    }
)
print(response.json())
```

## Next Steps

1. ✅ Service deployed and operational
2. ⚠️ Consider migrating credentials to Secret Manager (requires IAM permissions)
3. ✅ Test integration with other services
4. ✅ Monitor logs and metrics
5. ✅ Set up custom domain (optional)

## Deployment Date

**Deployed**: November 5, 2025  
**Deployment Method**: `gcloud run deploy --source .`  
**Status**: ✅ **PRODUCTION READY**
