# Toolbox (relocated)

Shared toolbox definitions moved to [`../neuro_san_studio/toolbox/toolbox_info.hocon`](../neuro_san_studio/toolbox/toolbox_info.hocon).

Set `AGENT_TOOLBOX_INFO_FILE` to that path (or rely on `run.py` / `ns run` defaults).
